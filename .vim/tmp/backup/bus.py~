BUS = [0,0,0,0]
# TODO: Could implement a class that raises an error if the size of
# the bus changes during the runtime. Would be useful for debugging.

def tri_state_logic(output, enable):
    if enable:
        BUS = output
